  // file handling in c++
  //computer work with file helps in storing data and information permanently.
// file is a bunch of bytes stored under a specific name on a storage device 
// stream refer to sequence on bytes ,every file is linkrd to a stream and each stream is associated with particular class 
// there are two type of file 
// 1. text file
// 2. binary file
// text file store information in ASCAII charecters , in text file each line of text is terminated by a special charecter known as EOL (end of line ) and delimiter charecter 
// when EOL charecter read or written the certain internal translations take place
// the binary file system contain infirmation in same format as it is held in memmory
//in binary file system no delimiter are used 
// binary files are faster and easier for program to read or write, by default files in c++ are considered as text file
// ifstram class is an input stream class, it provides the input operations for the files it inherits from the function porting randoms access to files and also provides member functions to perform file input operations seekg() and tellg() are used to perform random access to files which is defines in the ifstream class
// ofstream class is an output stream class, it provides the output operations for the files it inherits from the function porting randoms access to files and also provides member functions to perform file output operations seekp() and tellp() are used to perform random access to files which is defines 
// fsstream class is a file ia an input output stream class, it provides the input and output operations for files it inherits from the function porting random access to files and also provides member functions to perform file input and output operations seekg() and tellg() are used to perform random access to files which is defines in the fstream class
// creating an input stream :-
//  to create  an input stream you must delcaare the stream to be class if stream
//
// suntax :-
// ifstream fin;
// of stream fout;
// fstream fio;

// oppening a file using constructor :-
// constructor of stream  classes are used to initialize the stream and objects of stream classes can be initialized by passing the file name as an argument to the constructor
// syntax :-
// style; standar
// 1. 
// 2. alorgithms is generate function templete with is used 

// stl offers an assortment of contaniers 
// stl publishers the times ans storage complexity of its 's contaniers 
// 




// stl provide itebrators that make the containers an alorgithum fixeable 
// stl is a libra

// the user  alorgithum  can process 
// in stl the string is edit attraction during standard 
// in c++ standard provide of standard of class library name  string  class  
// there are three stl container classes ;
// 1. sequence: map, set. 
// 2. associative stack, queue, proliatity queue
// 
// the properties of vector class;
// 1 it provides list requresments concerties
// 2. efflicient subscriting is poosible and indices 
// 3. the list size is dynamic abnd can add time as an need.
// 4. the index checking is poosible through member function 
// 5. the iterator is used for efficient sequmental access.
